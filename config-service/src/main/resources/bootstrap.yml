server:
  port: 8081

spring:
  profiles:
    active: native
  cloud:
    config:
      server:
        git:
          uri: https://github.com/pablospfc/ms.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}

  security:
    user:
      name: root
      password: s3cr3t